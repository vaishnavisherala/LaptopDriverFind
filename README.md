# Laptop Drivers Fullstack

Contains backend (Node+TS) and frontend (Vite React+TS).

Start backend: cd backend && npm install && npm run dev
Start frontend: cd frontend && npm install && npm run dev
